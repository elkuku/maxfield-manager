{% extends 'base-map.html.twig' %}

{% block title %}
    {{ maxfield.name }}
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('map') }}
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('map') }}
{% endblock %}

{% block body %}
    <div
            id="map"
            data-maxfield-data='{{ jsonData|json_encode }}'
    ></div>

    <script>
        const gpxString = '{{ gpx|raw }}'
    </script>
{% endblock %}


